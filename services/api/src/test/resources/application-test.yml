spring:
  flyway:
    enabled: true
  jpa:
    hibernate:
      ddl-auto: validate
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ""

throughline:
  auth0:
    audience: https://api.throughline.app
    issuer-uri: ""
  seed:
    enabled: false
